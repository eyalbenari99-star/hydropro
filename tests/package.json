{
  "name": "hydronexis-payroll-tests",
  "private": true,
  "description": "Permanent regression suite for the attendance -> payroll chain. Run ./payroll/run.sh from this folder after any change to index.html that touches attendance, payroll, trips, approvals or exports.",
  "scripts": { "test": "bash payroll/run.sh" },
  "devDependencies": { "playwright-core": "^1.63.0" }
}
